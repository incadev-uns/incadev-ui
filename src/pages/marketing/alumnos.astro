---
import Layout from "../../layouts/MarketingLayout.astro";
---

<Layout title="Alumnos - INCADEV UI">
  <div class="space-y-6">
    <!-- Header -->
    <div class="flex items-center justify-between">
      <div>
        <h1 class="text-3xl font-bold text-gray-900 dark:text-white mb-2">
          Gestión de Alumnos
        </h1>
        <p class="text-gray-600 dark:text-gray-400">
          Administra el pipeline de leads y alumnos activos
        </p>
      </div>
      <button
        id="btnExportarDatos"
        class="px-6 py-3 bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-700 hover:to-purple-700 text-white rounded-lg font-medium smooth-transition flex items-center gap-2 shadow-lg hover:shadow-xl"
      >
        <svg
          class="w-5 h-5"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"
          ></path>
        </svg>
        Exportar Datos
      </button>
    </div>

    <!-- Stats Overview -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
      <div
        class="p-6 bg-gradient-to-br from-blue-50 to-blue-100 dark:from-blue-950/30 dark:to-blue-900/20 rounded-xl border border-blue-200 dark:border-blue-900/50"
      >
        <div class="flex items-center justify-between mb-2">
          <p class="text-sm font-medium text-blue-600 dark:text-blue-400">
            Total Alumnos
          </p>
          <svg
            class="w-8 h-8 text-blue-600 dark:text-blue-400"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"
            ></path>
          </svg>
        </div>
        <p
          id="stat-total"
          class="text-3xl font-bold text-gray-900 dark:text-white mb-1"
        >
          0
        </p>
        <p class="text-xs text-gray-600 dark:text-gray-400">Total en sistema</p>
      </div>

      <div
        class="p-6 bg-gradient-to-br from-green-50 to-green-100 dark:from-green-950/30 dark:to-green-900/20 rounded-xl border border-green-200 dark:border-green-900/50"
      >
        <div class="flex items-center justify-between mb-2">
          <p class="text-sm font-medium text-green-600 dark:text-green-400">
            Egresados
          </p>
          <svg
            class="w-8 h-8 text-green-600 dark:text-green-400"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M9 12l2 2 4-4M7.835 4.697a3.42 3.42 0 001.946-.806 3.42 3.42 0 014.438 0 3.42 3.42 0 001.946.806 3.42 3.42 0 013.138 3.138 3.42 3.42 0 00.806 1.946 3.42 3.42 0 010 4.438 3.42 3.42 0 00-.806 1.946 3.42 3.42 0 01-3.138 3.138 3.42 3.42 0 00-1.946.806 3.42 3.42 0 01-4.438 0 3.42 3.42 0 00-1.946-.806 3.42 3.42 0 01-3.138-3.138 3.42 3.42 0 00-.806-1.946 3.42 3.42 0 010-4.438 3.42 3.42 0 00.806-1.946 3.42 3.42 0 013.138-3.138z"
            ></path>
          </svg>
        </div>
        <p
          id="stat-egresados"
          class="text-3xl font-bold text-gray-900 dark:text-white mb-1"
        >
          0
        </p>
        <p class="text-xs text-gray-600 dark:text-gray-400">Con certificado</p>
      </div>

      <div
        class="p-6 bg-gradient-to-br from-purple-50 to-purple-100 dark:from-purple-950/30 dark:to-purple-900/20 rounded-xl border border-purple-200 dark:border-purple-900/50"
      >
        <div class="flex items-center justify-between mb-2">
          <p class="text-sm font-medium text-purple-600 dark:text-purple-400">
            Tasa Conversión
          </p>
          <svg
            class="w-8 h-8 text-purple-600 dark:text-purple-400"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
          </svg>
        </div>
        <p
          id="stat-conversion"
          class="text-3xl font-bold text-gray-900 dark:text-white mb-1"
        >
          0%
        </p>
        <p class="text-xs text-gray-600 dark:text-gray-400">Lead → Inscrito</p>
      </div>

      <div
        class="p-6 bg-gradient-to-br from-orange-50 to-orange-100 dark:from-orange-950/30 dark:to-orange-900/20 rounded-xl border border-orange-200 dark:border-orange-900/50"
      >
        <div class="flex items-center justify-between mb-2">
          <p class="text-sm font-medium text-orange-600 dark:text-orange-400">
            Tasa Deserción
          </p>
          <svg
            class="w-8 h-8 text-orange-600 dark:text-orange-400"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"
            ></path>
          </svg>
        </div>
        <p
          id="stat-desercion"
          class="text-3xl font-bold text-gray-900 dark:text-white mb-1"
        >
          0%
        </p>
        <p
          id="stat-desercion-label"
          class="text-xs text-gray-600 dark:text-gray-400"
        >
          Cargando...
        </p>
      </div>
    </div>

    <!-- Filtros por Estado -->
    <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-7 gap-4">
      <button
        class="filter-btn p-4 bg-white dark:bg-gray-950 border-2 border-blue-500 dark:border-blue-500 rounded-lg smooth-transition hover:shadow-md"
        data-filter="todos"
      >
        <div class="text-center">
          <p
            id="filter-todos"
            class="text-2xl font-bold text-gray-900 dark:text-white"
          >
            0
          </p>
          <p class="text-xs text-gray-600 dark:text-gray-400 mt-1 font-medium">
            Todos
          </p>
        </div>
      </button>

      <button
        class="filter-btn p-4 bg-white dark:bg-gray-950 border-2 border-gray-200 dark:border-gray-800 hover:border-yellow-500 dark:hover:border-yellow-500 rounded-lg smooth-transition hover:shadow-md"
        data-filter="pendiente"
      >
        <div class="text-center">
          <p
            id="filter-pendiente"
            class="text-2xl font-bold text-yellow-600 dark:text-yellow-400"
          >
            0
          </p>
          <p class="text-xs text-gray-600 dark:text-gray-400 mt-1 font-medium">
            Pendientes
          </p>
        </div>
      </button>

      <button
        class="filter-btn p-4 bg-white dark:bg-gray-950 border-2 border-gray-200 dark:border-gray-800 hover:border-blue-500 dark:hover:border-blue-500 rounded-lg smooth-transition hover:shadow-md"
        data-filter="cursando"
      >
        <div class="text-center">
          <p
            id="filter-cursando"
            class="text-2xl font-bold text-blue-600 dark:text-blue-400"
          >
            0
          </p>
          <p class="text-xs text-gray-600 dark:text-gray-400 mt-1 font-medium">
            Cursando
          </p>
        </div>
      </button>

      <button
        class="filter-btn p-4 bg-white dark:bg-gray-950 border-2 border-gray-200 dark:border-gray-800 hover:border-green-500 dark:hover:border-green-500 rounded-lg smooth-transition hover:shadow-md"
        data-filter="completado"
      >
        <div class="text-center">
          <p
            id="filter-completado"
            class="text-2xl font-bold text-green-600 dark:text-green-400"
          >
            0
          </p>
          <p class="text-xs text-gray-600 dark:text-gray-400 mt-1 font-medium">
            Completados
          </p>
        </div>
      </button>

      <button
        class="filter-btn p-4 bg-white dark:bg-gray-950 border-2 border-gray-200 dark:border-gray-800 hover:border-purple-500 dark:hover:border-purple-500 rounded-lg smooth-transition hover:shadow-md"
        data-filter="egresado"
      >
        <div class="text-center">
          <p
            id="filter-egresado"
            class="text-2xl font-bold text-purple-600 dark:text-purple-400"
          >
            0
          </p>
          <p class="text-xs text-gray-600 dark:text-gray-400 mt-1 font-medium">
            Egresados
          </p>
        </div>
      </button>

      <button
        class="filter-btn p-4 bg-white dark:bg-gray-950 border-2 border-gray-200 dark:border-gray-800 hover:border-red-500 dark:hover:border-red-500 rounded-lg smooth-transition hover:shadow-md"
        data-filter="desertor"
      >
        <div class="text-center">
          <p
            id="filter-desertor"
            class="text-2xl font-bold text-red-600 dark:text-red-400"
          >
            0
          </p>
          <p class="text-xs text-gray-600 dark:text-gray-400 mt-1 font-medium">
            Desertores
          </p>
        </div>
      </button>

      <button
        class="filter-btn p-4 bg-white dark:bg-gray-950 border-2 border-gray-200 dark:border-gray-800 hover:border-orange-500 dark:hover:border-orange-500 rounded-lg smooth-transition hover:shadow-md"
        data-filter="reprobado"
      >
        <div class="text-center">
          <p
            id="filter-reprobado"
            class="text-2xl font-bold text-orange-600 dark:text-orange-400"
          >
            0
          </p>
          <p class="text-xs text-gray-600 dark:text-gray-400 mt-1 font-medium">
            Reprobados
          </p>
        </div>
      </button>
    </div>

    <!-- Search and Filter Bar -->
    <div class="flex flex-col lg:flex-row gap-4">
      <div class="flex-1">
        <div class="relative">
          <svg
            class="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-gray-400"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
          </svg>
          <input
            id="searchInput"
            type="text"
            placeholder="Buscar por nombre o email..."
            class="w-full pl-12 pr-4 py-3 bg-white dark:bg-gray-950 border border-gray-300 dark:border-gray-800 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent text-gray-900 dark:text-white placeholder-gray-500 dark:placeholder-gray-400"
          />
        </div>
      </div>

      <select
        id="filterInteres"
        class="px-4 py-3 bg-white dark:bg-gray-950 border border-gray-300 dark:border-gray-800 rounded-lg text-gray-900 dark:text-white focus:ring-2 focus:ring-blue-500 focus:border-transparent"
      >
        <option value="">Todos los intereses</option>
        <option value="Inteligencia Artificial">Inteligencia Artificial</option>
        <option value="Cloud Computing">Cloud Computing</option>
        <option value="Ciberseguridad">Ciberseguridad</option>
        <option value="Marketing Digital">Marketing Digital</option>
        <option value="Gestión de Proyectos">Gestión de Proyectos</option>
      </select>

      <button
        id="btnLimpiarFiltros"
        class="px-6 py-3 border border-gray-300 dark:border-gray-700 text-gray-700 dark:text-gray-300 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-900 smooth-transition font-medium"
      >
        Limpiar
      </button>
    </div>

    <!-- Alumnos List -->
    <div id="alumnosList" class="grid grid-cols-1 lg:grid-cols-2 gap-4">
      <!-- Los alumnos se cargarán aquí dinámicamente -->
    </div>

    <!-- Empty State -->
    <div id="emptyState" class="hidden text-center py-12">
      <div
        class="w-20 h-20 bg-gray-100 dark:bg-gray-900 rounded-full flex items-center justify-center mx-auto mb-4"
      >
        <svg
          class="w-10 h-10 text-gray-400"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"
          ></path>
        </svg>
      </div>
      <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-2">
        No se encontraron alumnos
      </h3>
      <p class="text-gray-600 dark:text-gray-400">
        Intenta ajustar los filtros de búsqueda
      </p>
    </div>
  </div>

  <script>
    import { fetchAlumnosData, type AlumnoForUI, type StudentStatsForUI } from "../../services/marketing";

    // ============================================
    // CONFIGURACIÓN DE ESTADOS (según EnrollmentAcademicStatus)
    // ============================================
    const estadoConfig = {
      pendiente: {
        label: "Pendiente",
        icon: "🟡",
        bg: "bg-yellow-50 dark:bg-yellow-950/30",
        text: "text-yellow-700 dark:text-yellow-400",
        border: "border-yellow-200 dark:border-yellow-900/50",
      },
      cursando: {
        label: "Cursando",
        icon: "🔵",
        bg: "bg-blue-50 dark:bg-blue-950/30",
        text: "text-blue-700 dark:text-blue-400",
        border: "border-blue-200 dark:border-blue-900/50",
      },
      completado: {
        label: "Completado",
        icon: "✅",
        bg: "bg-green-50 dark:bg-green-950/30",
        text: "text-green-700 dark:text-green-400",
        border: "border-green-200 dark:border-green-900/50",
      },
      reprobado: {
        label: "Reprobado",
        icon: "❌",
        bg: "bg-orange-50 dark:bg-orange-950/30",
        text: "text-orange-700 dark:text-orange-400",
        border: "border-orange-200 dark:border-orange-900/50",
      },
      desertor: {
        label: "Desertor",
        icon: "🚫",
        bg: "bg-red-50 dark:bg-red-950/30",
        text: "text-red-700 dark:text-red-400",
        border: "border-red-200 dark:border-red-900/50",
      },
      egresado: {
        label: "Egresado",
        icon: "🎓",
        bg: "bg-purple-50 dark:bg-purple-950/30",
        text: "text-purple-700 dark:text-purple-400",
        border: "border-purple-200 dark:border-purple-900/50",
      },
    };

    // Variables globales
    let alumnos: AlumnoForUI[] = [];
    let stats: StudentStatsForUI | null = null;

    // ============================================
    // FUNCIONES AUXILIARES
    // ============================================
    function getDiasDesdeRegistro(fecha: string) {
      if (!fecha) return 0;
      const hoy = new Date();
      const registro = new Date(fecha);
      return Math.floor(
        (hoy.getTime() - registro.getTime()) / (1000 * 60 * 60 * 24),
      );
    }

    function getDiasUltimaInteraccion(fecha: string) {
      if (!fecha) return 0;
      const hoy = new Date();
      const ultima = new Date(fecha);
      return Math.floor(
        (hoy.getTime() - ultima.getTime()) / (1000 * 60 * 60 * 24),
      );
    }

    function getInitials(nombre: string) {
      return nombre
        .split(" ")
        .map((n) => n[0])
        .join("")
        .substring(0, 2)
        .toUpperCase();
    }

    // ============================================
    // ACTUALIZAR ESTADÍSTICAS (usando datos reales del API)
    // ============================================
    function updateStats() {
      if (!stats) return;

      const totalAlumnos = stats.totalMatriculados;

      // Calcular tasas
      const tasaCompletados = totalAlumnos > 0
        ? ((stats.completados / totalAlumnos) * 100).toFixed(1)
        : "0.0";
      const tasaDesercion = totalAlumnos > 0
        ? ((stats.desertores / totalAlumnos) * 100).toFixed(1)
        : "0.0";

      // Actualizar DOM - Stats principales
      const statTotal = document.getElementById("stat-total");
      const statEgresados = document.getElementById("stat-egresados");
      const statConversion = document.getElementById("stat-conversion");
      const statDesercion = document.getElementById("stat-desercion");
      const statDesercionLabel = document.getElementById("stat-desercion-label");

      if (statTotal) statTotal.textContent = String(totalAlumnos);
      if (statEgresados) statEgresados.textContent = String(stats.egresados);
      if (statConversion) statConversion.textContent = `${tasaCompletados}%`;
      if (statDesercion) statDesercion.textContent = `${tasaDesercion}%`;
      if (statDesercionLabel)
        statDesercionLabel.textContent =
          parseFloat(tasaDesercion) < 10 ? "Tasa baja ✓" : "Revisar";

      // Actualizar filtros con conteos reales
      const filterTodos = document.getElementById("filter-todos");
      const filterPendiente = document.getElementById("filter-pendiente");
      const filterCursando = document.getElementById("filter-cursando");
      const filterCompletado = document.getElementById("filter-completado");
      const filterEgresado = document.getElementById("filter-egresado");
      const filterDesertor = document.getElementById("filter-desertor");
      const filterReprobado = document.getElementById("filter-reprobado");

      if (filterTodos) filterTodos.textContent = String(totalAlumnos);
      if (filterPendiente) filterPendiente.textContent = String(stats.pendientes);
      if (filterCursando) filterCursando.textContent = String(stats.cursando);
      if (filterCompletado) filterCompletado.textContent = String(stats.completados);
      if (filterEgresado) filterEgresado.textContent = String(stats.egresados);
      if (filterDesertor) filterDesertor.textContent = String(stats.desertores);
      if (filterReprobado) filterReprobado.textContent = String(stats.reprobados);
    }

    // ============================================
    // RENDERIZAR ALUMNOS
    // ============================================
    interface Filtros {
      estado?: string;
      busqueda?: string;
      interes?: string;
    }

    function renderAlumnos(filtros: Filtros = {}) {
      const container = document.getElementById("alumnosList");
      const emptyState = document.getElementById("emptyState");
      if (!container) return;

      let filtered = alumnos;

      // Filtro por estado
      if (filtros.estado && filtros.estado !== "todos") {
        filtered = filtered.filter((a) => a.estado === filtros.estado);
      }

      // Filtro por búsqueda
      if (filtros.busqueda) {
        const busqueda = (filtros.busqueda ?? "").toLowerCase();
        filtered = filtered.filter(
          (a) =>
            a.nombre.toLowerCase().includes(busqueda) ||
            a.email.toLowerCase().includes(busqueda),
        );
      }

      // Filtro por interés
      if (filtros.interes) {
        const interes = filtros.interes;
        filtered = filtered.filter((a) =>
          a.interests.some((i) => i.includes(interes)),
        );
      }

      if (filtered.length === 0) {
        container.classList.add("hidden");
        emptyState?.classList.remove("hidden");
        return;
      }

      container.classList.remove("hidden");
      emptyState?.classList.add("hidden");

      container.innerHTML = filtered
        .map((a) => {
          const estado = estadoConfig[a.estado] || estadoConfig.pendiente;
          const diasRegistro = getDiasDesdeRegistro(a.fechaRegistro);
          const diasInteraccion = getDiasUltimaInteraccion(a.ultimaActualizacion);
          const initials = getInitials(a.nombre);

          return `
          <div class="p-6 bg-white dark:bg-gray-950 border border-gray-200 dark:border-gray-800 rounded-xl hover:shadow-lg smooth-transition group">
            <div class="flex items-start justify-between mb-4">
              <div class="flex items-center gap-4">
                ${
                  a.avatar
                    ? `
                  <img src="${a.avatar}" alt="${a.nombre}" class="w-14 h-14 rounded-full object-cover" />
                `
                    : `
                  <div class="w-14 h-14 bg-gradient-to-br from-blue-500 to-purple-500 rounded-full flex items-center justify-center text-white font-bold text-xl">
                    ${initials}
                  </div>
                `
                }
                <div>
                  <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-1 group-hover:text-blue-600 dark:group-hover:text-blue-400 smooth-transition">
                    ${a.nombre}
                  </h3>
                  <div class="flex items-center gap-2 text-sm text-gray-600 dark:text-gray-400">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                    </svg>
                    ${a.email}
                  </div>
                </div>
              </div>
              <span class="px-3 py-1 ${estado.bg} ${estado.text} border ${estado.border} rounded-full text-xs font-medium">
                ${estado.icon} ${estado.label}
              </span>
            </div>

            <div class="p-3 bg-gray-50 dark:bg-gray-900 rounded-lg mb-4">
              <div class="flex items-center justify-between mb-2">
                <div class="flex items-center gap-2">
                  <svg class="w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" />
                  </svg>
                  <span class="text-sm font-medium text-gray-900 dark:text-white">${a.curso}</span>
                </div>
                ${a.dni ? `<span class="text-xs text-gray-500 dark:text-gray-400">DNI: ${a.dni}</span>` : ''}
              </div>
              ${
                a.learningGoal
                  ? `
                <p class="text-xs text-gray-600 dark:text-gray-400 mt-2">
                  🎯 <span class="italic">${a.learningGoal}</span>
                </p>
              `
                  : ""
              }
            </div>

            ${
              a.interests && a.interests.length > 0
                ? `
              <div class="flex flex-wrap gap-2 mb-4">
                ${a.interests
                  .map(
                    (i) => `
                  <span class="px-2 py-1 bg-blue-50 dark:bg-blue-950/30 text-blue-600 dark:text-blue-400 text-xs rounded-full">
                    ${i}
                  </span>
                `,
                  )
                  .join("")}
              </div>
            `
                : ""
            }

            <div class="pt-3 border-t border-gray-200 dark:border-gray-800">
              <div class="flex items-center justify-between text-xs text-gray-600 dark:text-gray-400">
                <div class="flex items-center gap-4">
                  <span>Registrado hace ${diasRegistro} días</span>
                  ${
                    diasInteraccion <= 2
                      ? `
                    <span class="px-2 py-0.5 bg-green-50 dark:bg-green-950/30 text-green-600 dark:text-green-400 rounded-full font-medium">
                      Activo recientemente
                    </span>
                  `
                      : diasInteraccion <= 7
                        ? `
                    <span class="px-2 py-0.5 bg-yellow-50 dark:bg-yellow-950/30 text-yellow-600 dark:text-yellow-400 rounded-full font-medium">
                      Hace ${diasInteraccion} días
                    </span>
                  `
                        : `
                    <span class="px-2 py-0.5 bg-red-50 dark:bg-red-950/30 text-red-600 dark:text-red-400 rounded-full font-medium">
                      Inactivo (${diasInteraccion}d)
                    </span>
                  `
                  }
                </div>
                <span class="text-blue-600 dark:text-blue-400 font-medium">
                  ${a.telefono}
                </span>
              </div>
            </div>
          </div>
        `;
        })
        .join("");
    }

    // ============================================
    // CARGAR DATOS Y EVENT LISTENERS
    // ============================================
    document.addEventListener("DOMContentLoaded", async () => {
      console.log("[Alumnos] Loading students from API...");

      const container = document.getElementById("alumnosList");
      if (container) {
        container.innerHTML = `
          <div class="col-span-2 flex items-center justify-center py-12">
            <div class="text-center">
              <div class="animate-spin rounded-full h-12 w-12 border-b-2 border-blue-600 mx-auto mb-4"></div>
              <p class="text-gray-600 dark:text-gray-400">Cargando alumnos...</p>
            </div>
          </div>
        `;
      }

      try {
        const data = await fetchAlumnosData();
        alumnos = data.alumnos;
        stats = data.stats;
        console.log("[Alumnos] Students loaded:", alumnos.length);

        if (alumnos.length === 0) {
          console.warn("[Alumnos] No students returned from API");
        }

        updateStats();
        renderAlumnos();
      } catch (error) {
        console.error("[Alumnos] Error loading students:", error);
        if (container) {
          container.innerHTML = `
            <div class="col-span-2 text-center py-12">
              <div class="w-20 h-20 bg-red-100 dark:bg-red-900/30 rounded-full flex items-center justify-center mx-auto mb-4">
                <svg class="w-10 h-10 text-red-600 dark:text-red-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
              </div>
              <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-2">
                Error al cargar alumnos
              </h3>
              <p class="text-gray-600 dark:text-gray-400 mb-4">
                ${error instanceof Error ? error.message : "No se pudieron cargar los datos"}
              </p>
              <button onclick="window.location.reload()" class="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700">
                Reintentar
              </button>
            </div>
          `;
        }
        return;
      }

      let filtrosActuales: Filtros = { estado: "todos" };

      document.querySelectorAll(".filter-btn").forEach((btn) => {
        btn.addEventListener("click", () => {
          document.querySelectorAll(".filter-btn").forEach((b) => {
            b.classList.remove(
              "border-blue-500",
              "dark:border-blue-500",
              "border-green-500",
              "dark:border-green-500",
              "border-yellow-500",
              "dark:border-yellow-500",
              "border-purple-500",
              "dark:border-purple-500",
              "border-red-500",
              "dark:border-red-500",
              "border-orange-500",
              "dark:border-orange-500",
            );
            b.classList.add("border-gray-200", "dark:border-gray-800");
          });

          const filter = btn.getAttribute("data-filter");
          if (filter === "todos")
            btn.classList.add("border-blue-500", "dark:border-blue-500");
          else if (filter === "pendiente")
            btn.classList.add("border-yellow-500", "dark:border-yellow-500");
          else if (filter === "cursando")
            btn.classList.add("border-blue-500", "dark:border-blue-500");
          else if (filter === "completado")
            btn.classList.add("border-green-500", "dark:border-green-500");
          else if (filter === "egresado")
            btn.classList.add("border-purple-500", "dark:border-purple-500");
          else if (filter === "desertor")
            btn.classList.add("border-red-500", "dark:border-red-500");
          else if (filter === "reprobado")
            btn.classList.add("border-orange-500", "dark:border-orange-500");
          btn.classList.remove("border-gray-200", "dark:border-gray-800");

          filtrosActuales.estado = filter || "todos";
          renderAlumnos(filtrosActuales);
        });
      });

      const searchInput = document.getElementById("searchInput");
      searchInput?.addEventListener("input", (e) => {
        filtrosActuales.busqueda = (e.target as HTMLInputElement).value;
        renderAlumnos(filtrosActuales);
      });

      const filterInteres = document.getElementById("filterInteres");
      filterInteres?.addEventListener("change", (e) => {
        filtrosActuales.interes = (e.target as HTMLSelectElement).value;
        renderAlumnos(filtrosActuales);
      });

      const btnLimpiarFiltros = document.getElementById("btnLimpiarFiltros");
      btnLimpiarFiltros?.addEventListener("click", () => {
        filtrosActuales = { estado: "todos" };
        if (searchInput) (searchInput as HTMLInputElement).value = "";
        if (filterInteres) (filterInteres as HTMLSelectElement).value = "";

        document.querySelectorAll(".filter-btn").forEach((b) => {
          b.classList.remove(
            "border-blue-500",
            "dark:border-blue-500",
            "border-green-500",
            "dark:border-green-500",
            "border-yellow-500",
            "dark:border-yellow-500",
            "border-purple-500",
            "dark:border-purple-500",
            "border-red-500",
            "dark:border-red-500",
            "border-orange-500",
            "dark:border-orange-500",
          );
          b.classList.add("border-gray-200", "dark:border-gray-800");
        });
        document
          .querySelector('[data-filter="todos"]')
          ?.classList.add("border-blue-500", "dark:border-blue-500");
        document
          .querySelector('[data-filter="todos"]')
          ?.classList.remove("border-gray-200", "dark:border-gray-800");

        renderAlumnos(filtrosActuales);
      });

      const btnExportar = document.getElementById("btnExportarDatos");
      btnExportar?.addEventListener("click", () => {
        const csv = [
          [
            "ID",
            "Nombre",
            "Email",
            "DNI",
            "Telefono",
            "Estado",
            "Curso",
            "Intereses",
            "Objetivo",
            "Fecha Registro",
          ],
          ...alumnos.map((a) => [
            a.id,
            `"${a.nombre}"`,
            a.email,
            a.dni,
            a.telefono,
            a.estado,
            `"${a.curso}"`,
            `"${a.interests.join(', ')}"`,
            `"${a.learningGoal}"`,
            a.fechaRegistro?.split('T')[0] || '',
          ]),
        ]
          .map((row) => row.join(","))
          .join("\n");

        const blob = new Blob([csv], { type: "text/csv;charset=utf-8;" });
        const url = window.URL.createObjectURL(blob);
        const link = document.createElement("a");
        link.href = url;
        link.download = `alumnos_${new Date().toISOString().split("T")[0]}.csv`;
        link.click();
      });
    });
  </script>
</Layout>
